# Sample workflow for building and deploying a MkDocs site to GitHub Pages
name: Deploy

on:
  # Runs on pushes targeting the default branch
  push:
    branches:
      - master 
      - main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v2

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies for mkdocs
        run: pip install -r requirements.txt

      - name: Build site
        run: mkdocs build --config-file ./mkdocs.yml --site-dir ./_site

      - name: Debug _site directory
        run: ls -la ./_site

      - name: Install reprec used for replacing
        run: pip install reprec

      - name: Run replace for relative paths after build
        run: ./scripts/replace-relative-paths-build.sh

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site
          name: github-pages

      - name: List uploaded artifacts
        run: ls -la ${{ runner.temp }}/_github_home/_temp

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
        with:
          artifact_name: github-pages

